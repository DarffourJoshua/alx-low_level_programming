#include "main.h"

/**
 * _memset - a function that fills the buffer memory
 * @s: input parameter
 * @b: input parameter
 * @n: integer parameter
 * Return: the character
 */

char *_memset(char *s, char b, unsigned int n)
{
	return (s);
}
