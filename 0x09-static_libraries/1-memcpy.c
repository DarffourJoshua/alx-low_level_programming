#include "main.h"

/**
  * _memcpy - a function that copies the buffer memory
  * @dest: input string
  * @src: source parameter
  * @n: positive integer parameter
  * Return: a character
  */

char *_memcpy(char *dest, char *src, unsigned int n)
{
	char *start = src, *end = dest;
	return (end);
}
