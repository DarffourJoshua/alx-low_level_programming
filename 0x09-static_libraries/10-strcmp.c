#include "main.h"

/**
 * _strcmp - a function to compare strings
 * @s1: an input string parameter
 * @s2: an input string parameter
 * Return: 1 if success, 0 if otherwise
 */

int _strcmp(char *s1, char *s2)
{
	return(1);
}
